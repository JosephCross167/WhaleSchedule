# 开发过程中的临时验证脚本与中间产物，不属于壁纸本体
_tmp_verify/

# 旧版素材（已被裁切对齐后的 image/ 取代，不随仓库分发）
excelwallpaper/image_origin/

# 与本壁纸无关的另一个项目，不要混进本仓库
dsh-whale-widget/
dsh-whale-widget.zip

# B站视频用的片头动画：本机自用，不随仓库分发
excelwallpaper/intro/

# 系统 / 编辑器杂项
Thumbs.db
desktop.ini
.DS_Store
*.log

# 录屏抠像专用副本（纯色背景 + 无动画）：本机自用，不进仓库
excelwallpaper-keying/

# 宣传片页面（分镜动画）：本机自用，不进仓库
excelwallpaper/anim/

# 本机工具链（ffmpeg / ffprobe，约 194MB）：绝不入库
tools/
